# 협업 규칙

> 짧은 기간에 5명이 서로 다른 기능을 동시에 만듭니다.
> 규칙의 목적은 격식이 아니라 **충돌 방지와 진행 상황 공유**입니다. 부담되면 언제든 Discussions에 수정 제안해 주세요.

## ⚡ 3줄 요약

1. 모든 작업은 **이슈 → 브랜치 → PR** 순서로. 이슈 없이 브랜치 파지 않습니다.
2. `main`에 **직접 push 금지**. 반드시 PR로 병합합니다.
3. 하루 작업을 마치면 **자기 이슈에 코멘트 한 줄**을 남깁니다.

---

## 1. 작업 ID 체계

이슈 제목과 브랜치 이름에 공통으로 사용합니다.

| 접두사 | 영역 |
| --- | --- |
| `MATCH` | 닮은 동물 검색 (얼굴 임베딩 · 유사도 매칭) |
| `GAME` | 다마고찌 육성 게임 |
| `CHAT` | 동물과 대화하기 |
| `SYNTH` | 합성 사진 생성 및 공유 |
| `COMM` | 공통 (레이아웃, 배포, 환경 설정, DB 등) |
| `DOCM` | 문서 (기획서, 발표 자료, README) |

번호는 이슈 생성 순서대로 세 자리를 붙입니다. → `MATCH_001`, `GAME_003`

---

## 2. 이슈

- 템플릿 3종만 사용합니다: **Feature / Bug / Troubleshooting**
- 제목 형식: `[MATCH_001] 얼굴 임베딩 추출 함수 구현`
- **반쯤 진행된 작업도 이슈로 먼저 올립니다.** 이슈가 곧 진행 현황판입니다.
- 하나의 이슈는 **반나절 안에 끝나는 크기**로 자릅니다. 이틀짜리 이슈는 상태를 알 수 없어 쓸모가 없습니다.
- 트러블슈팅 이슈는 해결됐더라도 **닫지 말고 남겨두세요.** 발표 자료의 "문제 해결" 파트가 여기서 나옵니다.

### 라벨

| 종류 | 라벨 |
| --- | --- |
| 유형 | `feature` · `bug` · `troubleshooting` · `docs` |
| 영역 | `FE` · `BE` · `AI` |
| 급함 | `urgent` (진행이 막혔을 때만) |

---

## 3. 브랜치

```
main                 배포 · 발표 기준. 항상 동작하는 상태 유지
  └─ feat/MATCH_001-face-embedding
  └─ fix/GAME_012-feed-button
  └─ docs/DOCM_003-readme
```

- 형식: `타입/작업ID-짧은설명`
- 타입은 `feat` · `fix` · `refactor` · `docs` 네 가지만 사용합니다.
- 병합된 브랜치는 바로 삭제합니다.
- 남의 브랜치에 직접 push 하지 않습니다. 필요하면 그 사람 PR에 코멘트로 요청하세요.

### 접은 실험은 태그로 남깁니다

병합하지 않기로 한 브랜치를 그냥 지우면 커밋을 되찾을 수 없습니다. 지우기 전에 아카이브 태그를 답니다.

```bash
git tag -a archive/<브랜치명> origin/<브랜치명> -m "왜 접었는지 + 무엇이 들어있는지"
git push origin archive/<브랜치명>
git push origin --delete <브랜치명>
```

- 브랜치 목록은 깨끗해지고, 커밋은 영구 보존됩니다.
- 되살릴 때는 `git branch <새이름> archive/<브랜치명>` 한 줄이면 됩니다.
- 태그 메시지에 **왜 접었는지**를 꼭 적습니다. 몇 주 뒤에 그 이유를 아무도 기억하지 못합니다.

| 태그 | 내용 | 접은 이유 |
| --- | --- | --- |
| `archive/scene-habitats-by-biome` | 서식지별 확장 씬 5종 (초원·숲·사막·하늘·극지), `pet-scene.tsx` +235줄 | 고도화 일정 없음 |
| `archive/painterly-experiment` | 회화체 렌더링 4단계(단색·붓결·선 제거+빛·파스텔) + 꽃밭 씬, `lib/paint.ts` 신규 | 제품에 적용하지 않기로 함 |

---

## 4. 커밋

```
타입: 한 줄 요약 (50자 이내, 명령형)

- 상세 내용이 필요하면 본문에
- 관련 이슈: #12
```

**예시**

```
feat: 얼굴 임베딩 코사인 유사도 Top-5 추출 구현
fix: 밥 주기 버튼 연속 클릭 시 포만도 중복 증가 수정
refactor: 품종 이미지 로딩 로직 훅으로 분리
docs: 발표용 아키텍처 다이어그램 추가
```

**규칙**

- 한 커밋에 **한 가지 변경**만. "이것저것 수정" 커밋은 되돌릴 수 없습니다.
- `.env`, API 키, 모델 가중치, 학습용 이미지 원본은 **절대 커밋하지 않습니다.** 시작 전에 `.gitignore`를 먼저 확인하세요.
- 커밋 메시지 형식이 자꾸 헷갈리면 저장소 루트에서 한 번만 실행해두세요.

  ```bash
  git config commit.template .gitmessage
  ```

---

## 5. Pull Request

- 템플릿을 채워서 올립니다. 빈 PR은 리뷰하지 않습니다.
- 제목은 커밋 규칙과 동일: `feat: 얼굴 임베딩 Top-5 추출 구현`
- 본문에 `closes #이슈번호`를 넣으면 병합 시 이슈가 자동으로 닫힙니다.
- **승인 필수는 걸지 않습니다.** 기간이 짧아 승인 대기가 그대로 병목이 됩니다.
  - 대신 PR을 올린 뒤 **팀 채팅방에 링크를 공유**하고,
  - **30분 안에 반대 코멘트가 없으면 본인이 병합**합니다.
  - 단, `COMM` 영역(공통 레이아웃 · 배포 · DB 스키마)은 **반드시 1명 이상의 확인**을 받습니다. 여기서 깨지면 5명이 전부 멈춥니다.
- PR이 500줄을 넘어가면 리뷰가 불가능합니다. 기능을 쪼개서 올리세요.

---

## 6. 코드 리뷰 코멘트

지적의 무게를 앞에 붙여주세요. 짧은 프로젝트에서는 **"이건 꼭 고쳐야 하는 건가?"를 되묻는 시간**이 가장 아깝습니다.

| 접두사 | 의미 | 대응 |
| --- | --- | --- |
| `[P1]` | 병합 전에 반드시 수정 | 고치고 병합 |
| `[P2]` | 고치면 좋지만 지금은 아니어도 됨 | 이슈로 옮기고 병합 |
| `[P3]` | 단순 의견 · 취향 | 무시해도 됨 |
| `[Q]` | 질문 | 답변만 |

**예시**

```
[P1] 여기서 이미지 리사이즈를 안 하면 3MB 사진 올릴 때 임베딩이 타임아웃 납니다.
[P3] 이 변수명은 breedScore가 더 읽기 좋을 것 같아요.
[Q] 이 코사인 유사도 임계값 0.7은 어떤 기준으로 정하셨나요?
```

- 사람이 아니라 **코드**에 대해 씁니다. "왜 이렇게 했어요?"보다 "이 부분은 이런 경우에 문제가 될 것 같은데 어떻게 생각하세요?"
- 칭찬 코멘트도 남기세요. 남의 코드를 실제로 읽었다는 가장 확실한 증거입니다.

---

## 7. 기록

- **Decision Log** — 기술 선택이나 기획 변경이 있으면 Discussions에 남깁니다.
  형식: `무엇을 정했는가 / 후보는 무엇이었는가 / 왜 이걸 골랐는가`
  멘토링에서 나온 피드백과 그에 따른 변경도 여기 기록합니다. **발표 때 "왜 이렇게 만들었나"에 대한 답이 전부 여기서 나옵니다.**
- **막히면 2시간 안에 공유합니다.** 혼자 붙잡고 있는 시간이 전체 일정에서 가장 큰 손실입니다. 이슈에 `urgent` 라벨을 붙이거나 채팅방에 올려주세요.
